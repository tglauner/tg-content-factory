[project]
name = "tg-content-factory"
version = "0.1.0"
description = "Content generation for lectures and automated postings"
readme = "README.md"
requires-python = ">=3.10"

[project.scripts]
tg-content-factory = "tg_content_factory.cli:main"

[tool.setuptools.packages.find]
where = ["src"]
